<!DOCTYPE html>
<html>
<head>
    <title>Subqueries</title>
</head>
    <body>
        <h1>Correlated Subquery</h1>
        <p>This query lists the names of customers that have an order with a quantity greater than one.</p>
    <p>SELECT customer_name from customer where customer_id IN
         (select customer_id from order_ where quantity > 1);</P>

         <table border="1">
         <tr>
         <th>Customer Name</th>
         </tr>
         <% for(var i = 0; i < corr.length; i++) { %>
         <tr>
         <td><%= corr[i]._name %></td>
         </tr>
         <% } %>
         </table>
    </body>
</html>